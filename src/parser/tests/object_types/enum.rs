pub enum A {}
 pub enum B{}
pub enum C {

}
 enum D{

}


 pub enum E {

}
enum F {
    test,
    test2
}

pub enum G {
    test,
    test2,
}

pub enum H {
    test(String, Vec<u32>),
    test2 {
        bla: Vec<u32>,
    },
}